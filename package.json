{
  "name": "@vectronic/homebridge-twilio-sms",
  "displayName": "Twilio SMS",
  "version": "1.0.1",
  "description": "Homebridge plugin to send SMSes with a static message using Twilio API (paid service)",
  "license": "ISC",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "config.schema.json"
  ],
  "keywords": [
    "homebridge-plugin",
    "twilio",
    "homebridge",
    "sms"
  ],
  "engines": {
    "node": "^20.18.0 || ^22.10.0" || ^24.0.0",,
    "homebridge": "^1.8.0 || ^2.0.0-beta.0"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prepublishOnly": "npm run build"
  },
  "author": "vectronic",
  "repository": {
    "type": "git",
    "url": "git://github.com/vectronic/homebridge-twilio-sms.git"
  },
  "dependencies": {
    "twilio": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "homebridge": "^2.0.0-beta.55",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
